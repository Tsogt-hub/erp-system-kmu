# 📂 VS Code für ERP-System öffnen

## ✅ Schnellste Methode (Terminal):

### Schritt 1: Terminal öffnen
- **Mac**: Drücken Sie `Cmd + Leertaste`, tippen Sie "Terminal"
- **Oder**: Applications → Utilities → Terminal

### Schritt 2: Zum Projekt navigieren
```bash
cd '/Users/tsogtnandin-erdene/ERP System KMU'
```

### Schritt 3: VS Code öffnen
```bash
code .
```

**Das war's!** VS Code öffnet sich mit dem ERP-Projekt.

---

## 📁 Methode 2: VS Code direkt öffnen

### Schritt 1: VS Code starten
- Klicken Sie auf das VS Code Icon im Dock
- Oder: Spotlight (`Cmd + Leertaste`) → "Visual Studio Code"

### Schritt 2: Ordner öffnen
1. Klicken Sie in VS Code: **File → Open Folder...**
2. Navigieren Sie zu:
   ```
   /Users/tsogtnandin-erdene/ERP System KMU
   ```
3. Klicken Sie: **Open**

---

## 🖱️ Methode 3: Über Finder (Mac)

### Schritt 1: Finder öffnen
- Klicken Sie auf das Finder-Icon im Dock

### Schritt 2: Zum Projekt navigieren
1. Gehen Sie zu: `/Users/tsogtnandin-erdene/`
2. Finden Sie den Ordner: `ERP System KMU`

### Schritt 3: Mit VS Code öffnen
- **Rechtsklick** auf den Ordner
- Wählen Sie: **Open with Code**
- (Falls nicht sichtbar: Wählen Sie "Other..." und wählen Sie VS Code)

---

## ✅ Nach dem Öffnen in VS Code:

### Sie sollten sehen:

```
ERP System KMU/
├── backend/
├── frontend/
│   └── src/
│       └── pages/
│           └── Dashboard.tsx    ← Diese Datei öffnen!
├── STAGEWISE_OPTION1.md
└── ...
```

### Nächste Schritte:

1. **Im Explorer links**: Klicken Sie auf `frontend` → `src` → `pages` → `Dashboard.tsx`
2. **Stagewise starten**: `Cmd + Shift + P` → "Stagewise: Open Browser"
3. **Fertig!** Der Browser öffnet sich automatisch

---

## 🆘 Falls "code" Befehl nicht funktioniert:

### VS Code "code" Befehl installieren:

1. Öffnen Sie VS Code
2. Drücken Sie: `Cmd + Shift + P`
3. Tippen Sie: "Shell Command: Install 'code' command in PATH"
4. Enter drücken
5. Terminal neu starten
6. Jetzt funktioniert: `code .`

---

## 🎯 Projekt-Pfad:

**Vollständiger Pfad:**
```
/Users/tsogtnandin-erdene/ERP System KMU
```

**Wichtige Dateien:**
```
frontend/src/pages/Dashboard.tsx     ← Dashboard mit Charts
frontend/src/components/Layout.tsx   ← Navigation
frontend/src/pages/Projects.tsx      ← Projekte
```

---

## ✨ Nach dem Öffnen:

### Dashboard.tsx öffnen:
1. Explorer links: `frontend` → `src` → `pages` → `Dashboard.tsx`
2. Datei öffnet sich im Editor
3. Sie sehen den Code mit Charts und Gradient-Karten

### Stagewise starten:
1. `Cmd + Shift + P`
2. "Stagewise: Open Browser"
3. Browser öffnet sich mit Dashboard

**Viel Erfolg! 🚀**

